// ignore: file_names
import 'package:flutter/material.dart';

class EnprendimientoDigital extends StatelessWidget {
  const EnprendimientoDigital({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('4'),
    );
  }
}
